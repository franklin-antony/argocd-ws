# REFERENCE ONLY — non-stg1 configuration is intentionally disabled for MVP scope.
# Uncomment only when enabling this environment.
#
# # Kong Proxy TLS Certificate — dev environment
# #
# # Exposed externally via OpenShift Route (TLS passthrough).
# # Replace <APPS_DOMAIN> with your OpenShift wildcard apps domain.
# #
# # Secret:  kong-cp-testqa-np/kong-proxy-tls
# # Helm values:
# #   ssl_cert:     /etc/secrets/kong-proxy-tls/tls.crt
# #   ssl_cert_key: /etc/secrets/kong-proxy-tls/tls.key
#
# apiVersion: cert-manager.io/v1
# kind: Certificate
# metadata:
#   name: kong-proxy-cert
#   namespace: kong-cp-testqa-np
#   labels:
#     app.kubernetes.io/part-of: kong-gateway
#     app.kubernetes.io/component: data-plane
#     digitaldubai.ae/environment: testqa
# spec:
#   secretName: kong-proxy-tls
#   duration: 2160h    # 90 days
#   renewBefore: 360h  # 15 days before expiry
#   dnsNames:
#     - kong-proxy-testqa.<APPS_DOMAIN>
#   issuerRef:
#     name: kong-vault-issuer
#     kind: ClusterIssuer
#     group: cert-manager.io
